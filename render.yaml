services:
  - type: web
    name: smarthome-lighting-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: MCP_TRANSPORT
        value: streamable-http
